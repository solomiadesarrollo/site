import { Link } from 'react-router-dom';

import "./Navbar.css"

const Navbar = () => {

    return (
        <header>
            <nav className="navbar-container">
            </nav>
        </header>
    )
}

export default Navbar;